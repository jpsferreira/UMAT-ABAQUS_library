C
      DOUBLE PRECISION NELEM
      PARAMETER (NELEM=20804)
C  REAL PARAMETERS WITH FULL MACHINE PRECISION
      DOUBLE PRECISION ONE, TWO, THREE, FOUR, SIX, ZERO
      PARAMETER (ZERO=0.D0, ONE=1.0D0,TWO=2.0D0,THREE=3.0D0,FOUR=4.0D0,SIX=6.0D0)
      CHARACTER(256) DIR1, DIR2
      PARAMETER (DIR1='fibers.inp',DIR2='fibers.inp')
C  PARAMETERS FOR THE NONLOCAL DAMAGE AVERAGING
      INTEGER NUEL, NIPP
      PARAMETER (NUEL=NELEM, NIPP=KNPE)
      DOUBLE PRECISION DISTANCE
      PARAMETER (DISTANCE=5.0D0)
